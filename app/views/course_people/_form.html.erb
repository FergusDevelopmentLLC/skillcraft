<%= form_with(model: course_person, local: true) do |form| %>
  <% if course_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_person.errors.count, "error") %> prohibited this course_person from being saved:</h2>

      <ul>
        <% course_person.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :person_id %>
    <%= form.text_field :person_id %>
  </div>

  <div class="field">
    <%= form.label :classroom_id %>
    <%= form.text_field :classroom_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
