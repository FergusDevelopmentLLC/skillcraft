<%= form_with(model: topic, local: true) do |form| %>
  <% if topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
        <% topic.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <br/>
  <br/>
  <br/>

 <div class="field">
    <%= form.label :classroom%>
    <%= collection_select(:announcement, :classroom_id, Classroom.all, :id, :name, prompt: true) %>
  </div>

  <div class="field">
    <%= form.label :course%>
    <%= collection_select(:topic, :course_id, Course.all, :id, :name, prompt: true) %>
  </div>

  <div class="field">
    <%= form.label :topic%>
    <%= collection_select(:course, :topic_id, Topic.all, :id, :name, prompt: true) %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <br/>
  <hr/>
  <br/>
  
  <div class="actions">
    <%= form.submit %>
  </div>
  
<% end %>
