<div class="full">
  <main class="main">

    <p id="notice"><%= notice %></p>

    <% if @person.type == "Student" %>
    <h1>Editing Student</h1>
    <%else%>
    <h1>Editing Teacher</h1>
    <%end%>

    <% if @person && @person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@person.errors.count, "error") %> prohibited this person from being saved:</h2>
      <ul>
        <% @person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
      </ul>
    </div>
    <% end %>

    <%= form_with model: @person, local: true, method: "patch" do |form| %>
    
    <% if @person.type == "Student" %>
    <%= form.hidden_field :type, :value => 'Student'%>
    <%else%>
    <%= form.hidden_field :type, :value => 'Teacher'%>
    <%end%>

    <%= form.hidden_field :id, :value => @person.id%>
    
    <div class="field">
      <div class='avatar-wrapper'>
        <div class='avatar-container'>
          <%= cl_image_tag(@person.avatar.url)  %>
        </div>
        <a href="/people/<%=@person.id%>/randomize_avatar">randomize</a>
      </div>
    </div>

    <div class="field">
      <strong><%= form.label :user_name %>:</strong>
      <%= form.text_field :user_name %>
    </div>

    <div class="field">
      <strong><%= form.label :first_name %>:</strong>
      <%= form.text_field :first_name %>
    </div>

    <div class="field">
      <strong><%= form.label :last_name %>:</strong>
      <%= form.text_field :last_name %>
    </div>

    <div class="field">
      <strong><%= form.label :email %>:</strong>
      <%= form.text_field :email %>
    </div>

    <div class="field">
      <strong><%= form.label :Courses %>:</strong>
      <%if(@person.courses.count > 0)%>
        <% @person.courses.each do |course| %>
          <div><%=link_to course.name, course_path(course)%> < <%=link_to "Withdraw", course_person_path(course, @person) %></div>
        <% end %>
      <%else%>
        No courses
      <%end%>
    </div>

    <div class="field">
      <% if @person.type == "Student" %>
        <strong>Enroll in Course (enter course code):</strong>
      <%else%>
        <strong>Teach Course (enter course code):</strong>
      <%end%>
      <%= form.text_field :course_code %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>

    <% end %>

    <div class="actions">
      <% if @person.type == "Student" %>
        <%= link_to 'Students', students_path %> | <%= render :file => "./shared/_actions.html.erb" %><br />
      <%else%>
        <%= link_to 'Teachers', teachers_path %> | <%= render :file => "./shared/_actions.html.erb" %><br />
      <%end%>
    </div>

  </main>
</div>