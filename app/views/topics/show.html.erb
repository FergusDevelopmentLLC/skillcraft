<div class="full">
  <main class="main">
    <p id="notice"><%= notice %></p>

    <h1>Topic</h1>
    <p>
      <strong>Course:</strong>
      <%= @topic.course.name %>
    </p>

    <p>
      <strong>Title:</strong>
      <%= @topic.title %>
    </p>

    <div class="actions">
      <%= link_to 'Edit', edit_topic_path(@topic) %> |
      <%= link_to 'Back', topics_path %>
    </div>
  </main>
</div>